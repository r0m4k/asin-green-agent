[green_agent]
# This points to the Docker image you will build/push
image = "dolgopolyi/asin-green-agent:v1" 
env = { GOOGLE_MAPS_API_KEY = "${GOOGLE_MAPS_API_KEY}" }

[config]
domain = "spatial_navigation"
num_tasks = 10

